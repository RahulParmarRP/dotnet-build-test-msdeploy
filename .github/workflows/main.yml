name: Dotnet Build-Test-MSDeploy your project

on:
  push:
    branches: [ docker-file-windows-server ]

jobs:

  Build-Test-Deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Checking environment...
      uses: RahulParmarRP/dotnet-build-test-msdeploy@docker-file-windows-server